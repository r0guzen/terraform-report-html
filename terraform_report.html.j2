<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>Terraform Change Report</title>

<style>
:root {
  --green: #2ecc71;
  --yellow: #f1c40f;
  --red: #e74c3c;
  --bg: #f5f7fa;
  --card: #ffffff;
  --text: #2c3e50;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif;
  background: var(--bg);
  color: var(--text);
  margin: 0;
  padding: 24px;
}

h2 {
  margin-top: 0;
}

.group {
  background: var(--card);
  border-radius: 10px;
  margin-bottom: 18px;
  box-shadow: 0 2px 6px rgba(0,0,0,0.05);
}

.group summary {
  padding: 14px 18px;
  cursor: pointer;
  font-size: 16px;
  font-weight: 600;
  list-style: none;
}

.group summary::-webkit-details-marker {
  display:none;
}

.group-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.count {
  background: #e0e6ed;
  padding: 2px 10px;
  border-radius: 12px;
  font-size: 12px;
}

.resource {
  border-top: 1px solid #eee;
  padding: 14px 18px;
}

.resource:first-child {
  border-top: none;
}

.resource-card {
  border-left: 5px solid #ccc;
  padding-left: 12px;
}

.create { border-color: var(--green); }
.update { border-color: var(--yellow); }
.delete { border-color: var(--red); }

.resource-title {
  display: flex;
  justify-content: space-between;
  font-weight: 600;
  margin-bottom: 8px;
}

.badge {
  padding: 2px 8px;
  border-radius: 6px;
  color: white;
  font-size: 11px;
}

.create-badge { background: var(--green); }
.update-badge { background: var(--yellow); }
.delete-badge { background: var(--red); }

.cols {
  display: flex;
  gap: 16px;
  margin-top: 8px;
}

.col {
  width: 50%;
}

pre {
  background: #f7f9fc;
  padding: 10px;
  border-radius: 6px;
  overflow-x: auto;
  font-size: 12px;
}

details.inner summary {
  cursor: pointer;
  font-weight: 600;
  margin-bottom: 4px;
}

footer {
  margin-top: 40px;
  font-size: 12px;
  color: #888;
}
</style>
</head>

<body>

<h2>Terraform Change Report</h2>

{% if terraform_changes | length == 0 %}
<p>No effective changes detected.</p>
{% endif %}

{% for type, resources in terraform_changes | groupby("type") %}
<details class="group">
  <summary>
    <div class="group-header">
      <span>{{ type }}</span>
      <span class="count">{{ resources | length }}</span>
    </div>
  </summary>

  {% for r in resources %}
  {% set action = r.actions[0] %}
  <div class="resource">
    <div class="resource-card {{ action }}">
      <div class="resource-title">
        <span>{{ r.address }}</span>
        <span class="badge {{ action }}-badge">{{ action|upper }}</span>
      </div>

      <div class="cols">
        <div class="col">
          <details class="inner" open>
            <summary>Before</summary>
            <pre>{{ r.before | to_nice_json }}</pre>
          </details>
        </div>

        <div class="col">
          <details class="inner" open>
            <summary>After</summary>
            <pre>{{ r.after | to_nice_json }}</pre>
          </details>
        </div>
      </div>
    </div>
  </div>
  {% endfor %}
</details>
{% endfor %}

<footer>
Generated from Terraform plan — preview only. Final changes validated via FortiManager install preview.
</footer>

</body>
</html>
